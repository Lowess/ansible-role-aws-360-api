[uwsgi]
socket = :3031
stats = 127.0.0.1:1717

chdir = /opt/aws-360-api/.venv/lib/python3.5/site-packages

module = aws360api
callable = app

master = true

; Process is dynamically configured
; processes = 2
